{
  "name": "chess-app",
  "version": "1.0.0",
  "description": "Chess Application with Static Frontend and Spring Boot Backend",
  "main": "server.js",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo 'Test suite ready'"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": "14.x || 16.x || 18.x"
  }
}
